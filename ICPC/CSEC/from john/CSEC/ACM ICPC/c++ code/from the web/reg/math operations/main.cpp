#include <iostream>
#include <vector>

using namespace std;

int n, a, temp;
vector <int> comp;

int main()
{
    while (cin >> n && 2<=n && n<=10 && n!=-1)
    {
        cin >> a;
        for (int i=0; i<n; i++)
        {
            comp.push_back(temp);
        }
        while (true)
        {

        }
    }


}
