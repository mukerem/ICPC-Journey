#include <bits/stdc++.h>

using namespace std;

char maps[1000];

int main()
{
    while (true)
    {
        cin.getline (maps, 1001);
        if (maps[0] == '-' && maps[1] == '1')
        {
            return 0;
        }

    }
}
