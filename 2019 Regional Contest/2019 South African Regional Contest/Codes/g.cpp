#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
vector<vector<int>> v(50001, vector<int>(50001));
int main()
{
    ll x,r;
    ll n, m;
    while(cin >> n && n != -1){
        cin >> m;
        
    }
    return 0;
}